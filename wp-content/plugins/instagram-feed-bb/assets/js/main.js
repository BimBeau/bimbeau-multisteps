jQuery(document).ready(function($){
    // Initialisation du sélecteur de couleurs pour les champs de couleur
    $('.color-field').wpColorPicker();

    // Vous pouvez ajouter d'autres scripts ici à l'avenir
});
